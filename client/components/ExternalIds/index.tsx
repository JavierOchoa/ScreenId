import {FC, PropsWithChildren} from "react";
import {ExternalLinks} from "../../interfaces";

interface Props {
    externalData: ExternalLinks
}

const ExternalIds:FC<PropsWithChildren<Props>> = ({externalData}) => {
    return(
        <div>

        </div>
    )
}

export default ExternalIds;